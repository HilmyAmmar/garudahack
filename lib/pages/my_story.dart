import 'package:flutter/material.dart';

class MyStoryPage extends StatelessWidget {
  const MyStoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('MyStory'));
  }
}
